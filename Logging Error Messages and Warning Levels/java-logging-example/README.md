# Java Logging Example

This Maven project demonstrates how to log warning and error messages in Java using the built-in `java.util.logging` package.

## Run the example

From the project root, run:

```bash
mvn exec:java
```

## What it logs

- An informational message
- A warning message
- A severe error message
