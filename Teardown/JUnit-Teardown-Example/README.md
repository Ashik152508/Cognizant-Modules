# JUnit Teardown Example

This sample Maven project demonstrates a simple JUnit 5 teardown workflow using `@BeforeEach` and `@AfterEach`.

## Run the tests

```bash
mvn test
```

The test class shows how resources are prepared before each test and cleaned up afterward.
